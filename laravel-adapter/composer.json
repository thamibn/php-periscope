{
    "_comment": "Dev-only manifest. Source of truth for end users is the root /composer.json (thamibn/laravel-periscope). This file exists so contributors can `cd laravel-adapter && composer install` to run Pest in isolation.",
    "name": "thamibn/laravel-periscope-dev",
    "description": "Dev shadow of thamibn/laravel-periscope — DO NOT install from Packagist; the published package lives at repo root.",
    "type": "library",
    "license": "MIT",
    "keywords": ["laravel", "debugging", "observability", "telescope", "debugbar", "xdebug", "periscope"],
    "require": {
        "php": "^8.3",
        "illuminate/contracts": "^12.0 || ^13.0",
        "illuminate/support": "^12.0 || ^13.0"
    },
    "require-dev": {
        "laravel/mcp": "^0.7",
        "orchestra/testbench": "^10.0 || ^11.0",
        "pestphp/pest": "^3.0",
        "pestphp/pest-plugin-laravel": "^3.0"
    },
    "suggest": {
        "laravel/ai": "Enables AI-powered slow-query / N+1 / exception / error-log advisories via the AiAdvisor. Picks any provider — Ollama (free), OpenRouter (free tier), OpenAI, Anthropic, Gemini, Groq, etc.",
        "laravel/mcp": "Exposes periscope traces to AI agents (Claude / Cursor / Codex) via the Model Context Protocol. After install: `claude mcp add periscope -- php artisan mcp:start periscope`."
    },
    "autoload": {
        "psr-4": {
            "Periscope\\Laravel\\": "src/"
        }
    },
    "autoload-dev": {
        "psr-4": {
            "Periscope\\Laravel\\Tests\\": "tests/"
        }
    },
    "extra": {
        "laravel": {
            "providers": [
                "Periscope\\Laravel\\PeriscopeServiceProvider"
            ]
        }
    },
    "scripts": {
        "test": "pest"
    },
    "config": {
        "sort-packages": true,
        "allow-plugins": {
            "pestphp/pest-plugin": true
        }
    },
    "minimum-stability": "stable",
    "prefer-stable": true
}
